# AgentCash — private agent guardrails (local only)

**Copy this file to `Private.md` at the repo root** and edit `Private.md`. The real `Private.md` is **gitignored** and never pushed to GitHub.  
**Do not put production secrets here** if you ever copy this file; use `.env.local` and Cloudflare/CI secret stores for keys.

---

## Product

- **Name:** AgentCash  
- **Repo:** monorepo (`apps/web` — landing + health API; x402 task router in progress).

---

## Subdomains (maintain)

Official **AgentCash** host map — keep this table current when DNS, Workers, or Supabase redirect URLs change. Treat it as the source of truth for which hostname serves what.

| Hostname | Purpose |
|----------|---------|
| `agentcash.tech` | **Landing page** — marketing / public homepage |
| `app.agentcash.tech` | **Product** — this repo’s Next.js app (landing; dashboard/auth TBD) |
| `api.agentcash.tech` | **APIs** — x402 task router & programmatic entry (may share Worker with `app` initially) |
| `docs.agentcash.tech` | **Docs** — product & developer documentation site |
| `auth.agentcash.tech` | **Auth** — login / SSO / Supabase Auth callbacks when split from the main app |
| `status.agentcash.tech` | **Status page** — uptime & incident comms |

**Env:** set `NEXT_PUBLIC_SITE_URL` and auth callback URLs to the **canonical** origins you expose (often `https://app.agentcash.tech` until `api` / `auth` are isolated).

```mermaid
flowchart LR
  L["agentcash.tech (landing)"]
  P["app.agentcash.tech (product)"]
  A["api.agentcash.tech (APIs)"]
  D["docs.agentcash.tech (docs)"]
  U["auth.agentcash.tech (auth)"]
  S["status.agentcash.tech (status)"]
  L --> P
  P -.-> A
  U -.-> P
```

---

## Guardrails (fill in)

<!-- Example sections — replace with your rules -->

### Scope

- (e.g. “No drive-by refactors outside the task.”)
- (e.g. “Match existing patterns in `apps/web`.”)

### Naming & branding

- Prefer **AgentCash** in user-facing copy and new docs.

### Security

- Never commit `.env.local`, `Private.md` contents, or API keys into tracked files.

### Deploy

- Cloudflare Worker name: **agentcash** (`apps/web/wrangler.jsonc`).
- Build: `npm ci && npm run build` or `npm run build:cf`; deploy: `cd apps/web && npx wrangler deploy`.

---

## Active todos (agent)

<!-- Bullet list the agent should respect on the next turns -->

- [ ] (your item)

---

## Notes

(Space for freeform context the agent should read before building.)
